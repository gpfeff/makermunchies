<h1>Listing delivery_lists</h1>

<table>
  <thead>
    <tr>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @delivery_lists.each do |delivery_list| %>
      <tr>
        <td><%= link_to 'Show', delivery_list %></td>
        <td><%= link_to 'Edit', edit_delivery_list_path(delivery_list) %></td>
        <td><%= link_to 'Destroy', delivery_list, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Delivery list', new_delivery_list_path %>
