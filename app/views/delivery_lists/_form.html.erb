<%= form_for(@delivery_list) do |f| %>
  <% if @delivery_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@delivery_list.errors.count, "error") %> prohibited this delivery_list from being saved:</h2>

      <ul>
      <% @delivery_list.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
